import {expect, test} from '@oclif/test'

describe('generate', () => {
  test
  .it('generate', ctx => {
    ctx
    expect(true).to.equal(true)
  })
  // test
  // .stdout()
  // .command(['generate'])
  // .it('generate', ctx => {
  //   expect(ctx.stdout).to.contain('generate')
  // })
})
